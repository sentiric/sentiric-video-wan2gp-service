name: wan2gp-local-dev

networks:
  local-net:
    driver: bridge

volumes:
  local_rabbitmq_data_wan:
  local_minio_data_wan:
  local_video_wan_model_cache:

services:
  rabbitmq:
    image: ghcr.io/sentiric/sentiric-rabbitmq:latest
    container_name: local-rabbitmq-wan
    environment:
      - RABBITMQ_DEFAULT_USER=sentiric
      - RABBITMQ_DEFAULT_PASS=sentiric_pass
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - local-net
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: ghcr.io/sentiric/sentiric-minio:latest
    container_name: local-minio-wan
    environment:
      - MINIO_ROOT_USER=sentiric
      - MINIO_ROOT_PASSWORD=sentiric-secret-key
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    networks:
      - local-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/ready"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio-init:
    image: minio/mc
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - local-net
    entrypoint: >
      /bin/sh -c "
      mc alias set myminio http://minio:9000 sentiric sentiric-secret-key;
      mc mb myminio/sentiric --ignore-existing;
      mc anonymous set download myminio/sentiric;
      echo '✅ Local MinIO Bucket (sentiric) is ready.';
      "

  video-wan2gp-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: local-video-wan2gp-service
    environment:
      - ENV=development
      - WAN2GP_MODEL_ID=Wan-AI/Wan2.1-T2V-1.3B-Diffusers
      - GRPC_TLS_CA_PATH=/sentiric-certificates/certs/ca.crt
      - WAN2GP_SERVICE_CERT_PATH=/sentiric-certificates/certs/video-wan2gp-service-chain.crt
      - WAN2GP_SERVICE_KEY_PATH=/sentiric-certificates/certs/video-wan2gp-service.key
      - BUCKET_ENDPOINT_URL=http://minio:9000
      - BUCKET_ACCESS_KEY_ID=sentiric
      - BUCKET_SECRET_ACCESS_KEY=sentiric-secret-key
      - BUCKET_NAME=sentiric
      - RABBITMQ_URL=amqp://sentiric:sentiric_pass@rabbitmq:5672/%2f
    volumes:
      - "../sentiric-certificates:/sentiric-certificates:ro"
      - local_video_wan_model_cache:/app/model-cache
    ports:
      - "16120:16120"
      - "16121:16121"
    depends_on:
      rabbitmq:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - local-net
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
